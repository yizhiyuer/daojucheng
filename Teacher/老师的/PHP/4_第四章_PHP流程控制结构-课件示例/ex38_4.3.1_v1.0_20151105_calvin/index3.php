<?php
	$sum = 0;                            	//声明一个存储和的变量，初值为0
	for ( $i=1; $i <= 100; $i++ ) {
		if ($i%10 == 3)                  	//找到个位是3的数
			continue;                   	//跳过本次循环
		$sum += $i;                     	//累加结果
	}
	echo "结果为：$sum";                 	//输出结果为：4570

